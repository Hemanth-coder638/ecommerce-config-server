server:
  port: 4321

spring:
  application:
    name: shipping-service

eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka
  instance:
    prefer-ip-address: true
